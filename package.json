{
  "name": "MMM-GoogleKeepTester",
  "version": "1.0.0",
  "description": "Lists one Note from Google Keep",
  "main": "MMM-GoogleKeep.js",
  "author": "KoboseHome",
  "license": "MIT",
  "repository": {
    "type": "git"
  },
  "dependencies": {
    "python-shell": "latest"
  },
  "devDependencies": {
    "grunt": "latest",
    "grunt-eslint": "latest",
    "grunt-jsonlint": "latest",
    "grunt-markdownlint": "^1.0.13",
    "grunt-stylelint": "latest",
    "grunt-yamllint": "latest",
    "stylelint-config-standard": "latest",
    "time-grunt": "latest",
    "python-shell": "latest"
  }
  "scripts": {
    "preinstall": "sudo apt-get update && sudo apt-get install -y python3-pip && sudo apt-get install -y gkeepapi"
  }
}
